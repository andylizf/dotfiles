# Nix / Home Manager builds & symlinks
result*
*.drv

# Direnv
.direnv/

# macOS cruft
.DS_Store

# Secrets (never commit plaintext secrets; encrypted files only)
secrets/*.yaml
!secrets/secrets.example.yaml
!secrets/secrets.yaml
secrets/*.json
secrets/*.txt
secrets/*.env
*.agekey
.sops.yaml.local
